{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Docker Compose Up (Dev)",
      "type": "shell",
      "command": "docker-compose -f docker-compose.yml -f docker-compose.override.yml up --build",
      "problemMatcher": [],
      "group": "build",
      "detail": "Run Docker Compose for dev with hot reload"
    },
    {
      "label": "Docker Compose Up (Prod)",
      "type": "shell",
      "command": "docker-compose -f docker-compose.yml -f docker-compose.prod.yml up --build -d",
      "problemMatcher": [],
      "group": "build",
      "detail": "Run Docker Compose for production"
    }
  ]
}
